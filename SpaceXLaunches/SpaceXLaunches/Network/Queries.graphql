query LaunchList($limit: Int, $offset: Int) {
  launches(limit: $limit, offset: $offset) {
    id
    launch_date_utc
    mission_name
    rocket {
      rocket_name
      rocket_type
    }
    links {
      mission_patch_small
    }
  }
}
